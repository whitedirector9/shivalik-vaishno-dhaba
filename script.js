const navToggle=document.querySelector('.menu-toggle');const nav=document.querySelector('nav');navToggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');navToggle.setAttribute('aria-expanded',open)});const completeMenu=document.querySelector('.complete-menu');completeMenu.querySelectorAll('.menu-tabs button').forEach(button=>button.addEventListener('click',()=>{completeMenu.querySelector('.menu-tabs .active').classList.remove('active');button.classList.add('active');completeMenu.querySelectorAll('.menu-card').forEach(card=>card.hidden=button.dataset.filter!=='all'&&card.dataset.category!==button.dataset.filter)}));
